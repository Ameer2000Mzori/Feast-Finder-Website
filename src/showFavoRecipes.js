// selecting elements
const bookMarksBox = document.getElementById('book-Marks-Box')

// gelobal variables

// functions

// event listeners
bookMarksBox.addEventListener('click', (e) => {
  console.log('clicked')
})
